To run the script: `ansible-playbook --ask-become-pass <instance name>.yml`

To start fresh:

```bash
wget https://raw.githubusercontent.com/arusahni/provisioning/master/bootstrap.sh -O /tmp/bootstrap.sh
sudo bash /tmp/bootstrap.sh
```
