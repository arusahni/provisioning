---
- name: Install Arch packages
  become: true
  pacman: pkg={{ item }}
  with_items:
    - obs-studio

- name: Install AUR packages
  become: true
  yay: name={{ item }} state=present
  with_items:
    - zoom
    - v4l2ucp
    - 1password
